/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['pbs.twimg.com','images.pexels.com', "img.clerk.com", "utfs.io"],
      },
};

export default nextConfig;
